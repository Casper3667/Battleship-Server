using GameServer.Controllers;

namespace GameTest
{
    public class Tests
    {
        [SetUp]
        public void Setup()
        {
        }

        /// <summary>
        /// This Test Has Been Removed Since a method used in it made the controller break
        /// </summary>
        //[Test]
        //public void Test1()
        //{
        //    ServerHealth serv = new();
        //    int players = serv.Get();
        //    int expectedPlayers = 0;

        //    Assert.NotNull(players);
        //    Assert.That(expectedPlayers, Is.EqualTo(players));
        //}
    }
}